CREATE TABLE PRESCRIPCION(
ID_PRESCRIPCION INT IDENTITY,
ID_RECORDS_PRESCRIPCION VARCHAR(7)NOT NULL,
ID_MEDICO VARCHAR(13) NOT NULL,
PRESCRIPCION_ALM VARCHAR(1) NOT NULL,
FECHA_CREADO_PRESCRIPCION DATE NOT NULL,
PRIMARY KEY(ID_PRESCRIPCION)
)