CREATE TABLE USUARIOS(
IDUSU INT IDENTITY (1,1)not null
,APODO VARCHAR(25)NOT NULL
,CLAVE VARCHAR(25)NOT NULL
,ID_PERSONAL_CENTRO INT NOT NULL
,LOGEADO CHAR(1)
,FECHA_ULTIMO_SESION DATETIME
,FECHA_CREADO DATETIME NOT NULL
,SKIN INT
PRIMARY KEY(ID_PERSONAL_CENTRO),
CONSTRAINT FK_IDPERSONAL_C_IDCP FOREIGN KEY(ID_PERSONAL_CENTRO)REFERENCES PERSONAL_CENTRO(IDPC)) 
/*drop table usuarios*/

