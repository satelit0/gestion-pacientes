CREATE table DETALLE_PRESCRIPCION(
IDDETALLE INT IDENTITY,
ID_PRESCRIPCION_DETALLE INT NOT NULL,
NOMBBRE_MEDICAMENTO VARCHAR(25) NOT NULL,
CANTIDAD VARCHAR(10)NOT NULL,
UNIDAD VARCHAR(10) NOT NULL,
MODO_CONSUMO VARCHAR(35)NOT NULL,
EN_CANTIDAD_1 VARCHAR(15),
EN_CANTIDAD_2 VARCHAR(15),
NOTA VARCHAR(255),
CONSUMO_ACTIVO VARCHAR(10) NOT NULL,
REEMPLAZO_PERMITIDO VARCHAR(10) NOT NULL,
FECHA_INICIO_CONSUMO DATETIME
PRIMARY KEY(IDDETALLE))


ALTER TABLE DETALLE_PRESCRIPCION
ADD CONSTRAINT FK_IDPRESCRIPCION_DETALLE_PRES FOREIGN KEY(ID_PRESCRIPCION_DETALLE)REFERENCES PRESCRIPCION(IDPRESCRIPCION)

/*DROP TABLE DETALLE_PRESCRIPCION*/